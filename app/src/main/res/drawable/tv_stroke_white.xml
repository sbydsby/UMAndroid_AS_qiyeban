<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" >
    <!-- 实心 -->
    <solid android:color="@android:color/transparent" />
    <!-- 边框 -->
    <stroke
        android:width="1.5dp"
        android:color="@color/cert_type_text" />
    <!-- 圆角 -->
    <corners android:radius="3dp" />
    <!-- 边距 -->
    <padding
        android:bottom="3dp"
        android:left="3dp"
        android:right="3dp"
        android:top="3dp" />
</shape>